targetCustomizations:
- name: gke
  helm:
    values:
      rabbitmqServer:
        persistence:
          storageClassName: premium-rwo
  clusterSelector:
    matchLabels:
      provider: gke
- name: openshift
  helm:
    values:
      ingress:
        host: rmq-demo.apps.os2.openshift.edge-clusters.net
  clusterSelector:
    matchLabels:
      provider: openshift
