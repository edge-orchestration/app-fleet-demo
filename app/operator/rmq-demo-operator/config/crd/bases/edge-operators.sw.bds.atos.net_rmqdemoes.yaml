---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: rmqdemoes.edge-operators.sw.bds.atos.net
spec:
  group: edge-operators.sw.bds.atos.net
  names:
    kind: RmqDemo
    listKind: RmqDemoList
    plural: rmqdemoes
    singular: rmqdemo
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RmqDemo is the Schema for the rmqdemoes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RmqDemoSpec defines the desired state of RmqDemo
            properties:
              consumer:
                description: Consumer App
                properties:
                  port:
                    default: 4002
                    description: Port is the port number to expose on the Consumer
                      Pod
                    format: int32
                    type: integer
                  replicas:
                    default: 1
                    description: Replicas is the number of deployement replicas to
                      scale
                    format: int32
                    type: integer
                type: object
              forceRedeploy:
                description: force Re-deploy
                type: boolean
              publisher:
                description: Publisher App
                properties:
                  port:
                    default: 4001
                    description: Port is the port number to expose on the Publisher
                      Pod
                    format: int32
                    type: integer
                  replicas:
                    default: 1
                    description: Replicas is the number of deployement replicas to
                      scale
                    format: int32
                    type: integer
                type: object
              rabbitmq:
                description: Rabbitmq Server
                properties:
                  replicas:
                    default: 1
                    description: Replicas is the number of deployement replicas to
                      scale
                    format: int32
                    type: integer
                type: object
            type: object
          status:
            description: RmqDemoStatus defines the observed state of RmqDemo
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
